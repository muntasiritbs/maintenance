// Loader.jsx
import React from 'react';
import './Loader.css'; // Ensure the loader styles are imported

const Loader = () => (
  <div className="loader-wrapper">
    <div className="loader"></div>
  </div>
);

export default Loader;
